{%
  set classes = [
    'view',
    'view-' ~ id|clean_class,
    'view-id-' ~ id,
    'view-display-id-' ~ display_id,
    dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
%}

{# 
<div{{ attributes.addClass(classes) }}>
{% if rows %}
  {% if id == "homapage_bootstrap" %}
    <section class="about">
      <div class="row">
        {{ rows }}
      </div>
    </section>

  {% elseif id == "students" %}
    <section class="customers">   
      <div class="container">
        <div class="row">
          <div class="col-md-8 col-md-offset-2">
            <div class="carousel slide" id="fade-quote-carousel">
              <div class="carousel-inner" role="listbox">
                  {{ rows }}
               </div>    
                   <a class="left carousel-control" href="#fade-quote-carousel" role="button" data-slide="prev">
                    <i class="fa fa-angle-left" aria-hidden="true"></i>
                    <span class="sr-only">Previous</span>
                  </a>
                  <a class="right carousel-control" href="#fade-quote-carousel" role="button" data-slide="next">
                    <i class="fa fa-angle-right" aria-hidden="true"></i>
                    <span class="sr-only">Next</span>
                  </a>
            </div>              
          </div>
        </div>  
      </div>
    </section>
  {% elseif id == "tourist_spots" %}
      <section id="services">
        <div class="container">
          {{ rows }}
        </div>
      </section>
  {% elseif id == "schools" %}
      <section id="blog">
        <div class="container-fluid">
          <div class="row">
              <h4>Explore</h4>
              <h6>Explore the Beauty and Location of Our Schools in Dauin</h6>
              {{ rows }}
          </div>
        </div>
      </section>        
  
  {% endif %} 
{% endif %}  



  {% if header %}
    {% if id == "homapage_bootstrap" %} 
      {{ header }}
    {% endif %}    
  {% endif %}  

  {% if footer %}
     <section class="team">
        <h4>Upcoming Events</h4>
        <h6>These is Testing for upcoming events</h6>
        <div class="row">
          {{ footer }}
        </div>
      </section>
  {% endif %}


{% if pager %}
  <section id="blog">
    <div class="container-fluid">
      <div class="row">
        {{ pager }}
      </div>
    </div>
  </section>
{% endif %}

</div>
 #}
<div{{ attributes.addClass(classes) }}>

{% if id == "homapage_bootstrap" %}
  {% if rows %}
    <section class="about">
      <div class="row">
        {{ rows }}
      </div>
    </section>  
    {# {{ rows }} #}
  {% endif %}  

  {% if header %}
    {{ header }}
  {% endif %} 

  {% if footer %}
    <section class="team">
      <h4>Upcoming Events</h4>
      <h6>These is Testing for upcoming events</h6>
      <div class="row">
          {{ footer }}
      </div>
    </section>
  {% endif %}

{% endif %} 

 


{% if id == "students" %}
  {% if rows %}
    <section class="customers">   
      <div class="container">
        <div class="row">
          <div class="col-md-8 col-md-offset-2">
            <div class="carousel slide" id="fade-quote-carousel">
              <div class="carousel-inner" role="listbox">
                  {{ rows }}
               </div>    
                   <a class="left carousel-control" href="#fade-quote-carousel" role="button" data-slide="prev">
                    <i class="fa fa-angle-left" aria-hidden="true"></i>
                    <span class="sr-only">Previous</span>
                  </a>
                  <a class="right carousel-control" href="#fade-quote-carousel" role="button" data-slide="next">
                    <i class="fa fa-angle-right" aria-hidden="true"></i>
                    <span class="sr-only">Next</span>
                  </a>
            </div>              
          </div>
        </div>  
      </div>
    </section>
  {% endif %}
{% endif %}


{% if id == "tourist_spots" %}
  {% if rows %}
      <section id="services">
        <div class="container">
          {{ rows }}
        </div>
      </section>
  {% endif %}
{% endif %}      
  

{% if id == "schools" %}
  {% if rows %}
    <section id="blog">
      <div class="container-fluid">
        <div class="row">
            <h4>Explore</h4>
            <h6>Explore the Beauty and Location of Our Schools in Dauin</h6>
            {{ rows }}
        </div>
      </div>
    </section> 
  {% endif %}
{% endif %}

{% if pager %}
  <section id="footer-blog">
    <div class="container-fluid">
      <div class="row">
        {{ pager }}
      </div>
    </div>
  </section>
{% endif %}  

</div>
